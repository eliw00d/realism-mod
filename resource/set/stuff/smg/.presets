(include "\set\small.firearms.accuracy")

(define "range"
	{range (* 3.75 %r) (* 3.75 %r)}
)

(define "speed"   
	{speed (* 0.5 %s)}  
)

(define "recoveryTime"   
	{recoveryTime (/ 60 %r)}
)

(define "burst_short"
	{burst
		{short 5 0.5}
		{long 8 0.5}
	}
)

(define "burst_medium"
	{burst
		{short 7 0.5}
		{long 12 0.5}
	}
)

(define "burst_long"
	{burst
		{short 9 0.5}
		{long 25 0.5}
	}	
)
